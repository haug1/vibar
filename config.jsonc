{
  // Optional. Remove this file to use built-in defaults.
  areas: {
    left: [
      { type: "workspaces" }
    ],
    center: [
      // interval_secs defaults to 5 and minimum is 1
      // { type: "exec", command: "echo hello", interval_secs: 2 }
    ],
    right: [
      { type: "clock", format: "%a %d. %b %H:%M:%S" }
    ]
  }
}
